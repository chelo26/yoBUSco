<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:padding="@dimen/spacing_normal"
    tools:context=".SearchResultsActivity">

    <TextView
        android:id="@+id/search_result_list_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAllCaps="true"
        android:text="Eleje tiempo de la partida:"
        style="@style/app_text_normal"
        />

    <View style="@style/divider_black"
        android:id="@+id/search_result_list_header_divider"
        android:layout_below="@id/search_result_list_header"
        android:layout_marginBottom="@dimen/spacing_tiny"/>

    <ListView
        android:id="@android:id/list"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/search_result_list_header_divider"
        />

    <TextView
        android:id="@android:id/empty"
        android:text="@string/list_is_empty"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:textSize="@dimen/font_larger"
        android:gravity="center_vertical|center_horizontal"
        />

</RelativeLayout>
